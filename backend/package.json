{
  "name": "laganga-backend",
  "version": "1.0.0",
  "description": "Backend proxy server for La Ganga Dashboard – handles Kick OAuth2, API calls requiring the Client Secret, and webhook verification.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18"
  }
}
